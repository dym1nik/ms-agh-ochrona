services:
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: msagh_php
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: dev
    depends_on:
      - db

  nginx:
    image: nginx:1.27-alpine
    container_name: msagh_nginx
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  db:
    image: postgres:16-alpine
    container_name: msagh_db
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: